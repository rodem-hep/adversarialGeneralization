# @package _global_

# Order indicates overwriting
defaults:
  - training_modes/ML_training_b256.yaml
  - override /model: biconv_CLR.yaml
  - override /datamodule: ji_doublet_data.yaml

project_name: JetCLIP # Determines output directory path and wandb project
network_name: biconvCLR_diffV0.2 # Used for both saving and wandb
